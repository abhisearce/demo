apiVersion: v1
kind: Service
metadata:
  name: kpt-tech
  namespace: example-ns
  labels:
    type: demo
    app: dry-run
spec:
  type: LoadBalancer
  selector:
    type: demo
    app: dry-run
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
